pipeline {
    agent any

    parameters {
        string(name: 'URL', description: 'URL to open')
        string(name: 'TEXT', description: 'Text to verify')
        string(name: 'USERNAME', description: 'Enter the username' )
        string(name: 'PASSWORD', description: 'Enter the password')
    }

    environment {
        PATH = "C:\\Program Files\\nodejs;${env.PATH}"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                bat 'npm ci'
                bat 'npx playwright install'
            }
        }

        stage('Run Playwright Tests') {
            steps {
                bat """
                    set URL=${params.URL}
                    set TEXT=${params.TEXT}
                    set USERNAME=${params.USERNAME}
                    set PASSWORD=${params.PASSWORD}
                    set PLAYWRIGHT_DISABLE_COLORS=1
                    npx playwright test
                """
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'test-results/**', allowEmptyArchive: true
            archiveArtifacts artifacts: 'playwright-report/**', allowEmptyArchive: true
        }
    }
}
